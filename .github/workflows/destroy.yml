name: Destroy ABC Startup Infrastructure

# ----------------------------------------
# MANUAL trigger only
# Destructive action – never auto-run
# ----------------------------------------
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  destroy:
    name: Terraform Destroy
    runs-on: ubuntu-latest

    steps:
      # -----------------------------------
      # Checkout repository code
      # -----------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------------
      # Authenticate to AWS using OIDC
      # Uses the same single IAM role
      # -----------------------------------
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::208107893850:role/github-terraform-role
          aws-region: ap-south-1

      # -----------------------------------
      # Install Terraform CLI
      # -----------------------------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # -----------------------------------
      # Initialize backend and destroy infra
      # -----------------------------------
      - name: Terraform Destroy
        run: |
          terraform init -input=false
          terraform destroy -auto-approve
